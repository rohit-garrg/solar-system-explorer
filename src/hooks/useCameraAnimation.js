/**
 * Lerp-based camera animation hook.
 * Smoothly transitions camera position and lookAt target
 * when a body is selected or deselected.
 */
export default function useCameraAnimation() {
  // TODO: useFrame with lerp for position + lookAt transitions
  return {
    isAnimating: false,
  }
}
