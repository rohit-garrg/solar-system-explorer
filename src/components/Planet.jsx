/**
 * Reusable planet component.
 * Handles: orbital group > tilt group > sphere + optional clouds + moons.
 * All positioning uses scene graph hierarchy (nested groups).
 */
export default function Planet() {
  // TODO: Orbital rotation, axial tilt, textured sphere, moons
  return null
}
