/**
 * Spacecraft — rocket geometry that flies between planets on a bezier path.
 * Visible only when spacecraft mode is active.
 */
export default function Spacecraft() {
  // TODO: Rocket mesh, bezier flight path, visibility toggle
  return null
}
