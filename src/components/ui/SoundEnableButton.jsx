/**
 * First-load audio opt-in button.
 * Browser autoplay policy requires user interaction before AudioContext.
 */
export default function SoundEnableButton() {
  // TODO: "Turn on Sound" button, AudioContext init on tap
  return null
}
