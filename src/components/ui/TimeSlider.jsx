/**
 * Time speed control — range slider (0.1x to 20x) with play/pause.
 * Fixed to bottom center. Keyboard: Space = play/pause, arrows = speed.
 */
export default function TimeSlider() {
  // TODO: Range input, play/pause button, speed label
  return null
}
