/**
 * Fact card overlay — slides in from right (desktop) or up (mobile).
 * Shows body name, fun fact, stats, and navigation.
 */
export default function FactCard() {
  // TODO: Animated slide-in, fact content, next/dismiss buttons
  return null
}
