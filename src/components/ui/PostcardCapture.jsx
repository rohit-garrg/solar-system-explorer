/**
 * Postcard screenshot feature — captures canvas and adds a decorative frame.
 * Requires preserveDrawingBuffer on the Canvas.
 */
export default function PostcardCapture() {
  // TODO: Screenshot capture, frame overlay, download
  return null
}
