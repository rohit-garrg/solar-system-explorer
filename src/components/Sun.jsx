/**
 * The Sun — emissive sphere + point light + bloom glow.
 * Uses MeshBasicMaterial (self-lit, not affected by scene lights).
 */
export default function Sun() {
  // TODO: Emissive sphere, point light, bloom effect
  return null
}
